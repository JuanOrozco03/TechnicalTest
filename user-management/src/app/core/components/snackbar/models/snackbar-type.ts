export enum SnackbarType {
    success = 'success',
    error = 'error',
    warning = 'warning',
  }
  
  export enum SnackbarPrimaryColor {
    success = '#dff6dd',
    error = '#fde7e9',
    warning = '#fff4ce',
  }
  
  export enum SnackbarAuxColor {
    success = '#3f7e15',
    error = '#a80000',
    warning = '#76716f',
  }
  
  export enum SnackbarIcon {
    success = 'check_circle_outline',
    error = 'error_outline',
    warning = 'warning_amber',
  }
  